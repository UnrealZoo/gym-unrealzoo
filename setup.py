from setuptools import setup

setup(name='gym_unrealcv',
      version='1.0.0',
      install_requires=['gym<=0.21.0', 'matplotlib', 'numpy', 'unrealcv', 'wget', 'opencv-python','simple_pid'],  # And any other dependencies foo needs
)
